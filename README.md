Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

src/ — исходные файлы проекта

src/components/ — папка с JS компонентами

src/components/base/ — папка с базовым кодом

Важные файлы:

index.html — HTML-файл главной страницы

src/types/index.ts — файл с типами

src/main.ts — точка входа приложения

src/scss/styles.scss — корневой файл стилей

src/utils/constants.ts — файл с константами

src/utils/utils.ts — файл с утилитами

Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

npm install
npm run dev


или

yarn
yarn dev


Сборка

npm run build


или

yarn build


Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.

View - слой представления, отвечает за отображение данных на странице.

Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

Базовый код

Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной T тип данных, которые могут быть переданы в метод render для отображения.

Конструктор:

constructor(container: HTMLElement) - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:

container: HTMLElement - поле для хранения корневого DOM элемента компонента.

Методы класса:

render(data?: Partial<T>): HTMLElement - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от Component будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова render и записывать данные в необходимые DOM элементы.

setImage(element: HTMLImageElement, src: string, alt?: string): void - утилитарный метод для модификации DOM-элементов <img>

Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:

constructor(baseUrl: string, options: RequestInit = {}) - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:

baseUrl: string - базовый адрес сервера

options: RequestInit - объект с заголовками, которые будут использованы для запросов.

Методы:

get(uri: string): Promise<object> - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер

post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object> - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется POST запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.

handleResponse(response: Response): Promise<object> - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:

_events: Map<string | RegExp, Set<Function>>) -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:

on<T extends object>(event: EventName, callback: (data: T) => void): void - подписка на событие, принимает название события и функцию обработчик.

emit<T extends object>(event: string, data?: T): void - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.

trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

Типы данных (src/types/index.ts)

Тип IProduct

Назначение: Описывает структуру товара в каталоге.

Поля:

id: string - Уникальный идентификатор товара.

description: string - Полное описание товара.

image: string (URL) - Ссылка на изображение.

title: string - Название товара.

category: string - Категория товара.

price: number | null - Цена товара.

Тип ICartItem

Назначение: Описывает элемент в корзине.

Поля:

id: string - Идентификатор товара.

title: string - Название товара.

price: number - Цена товара.

Тип IOrder

Назначение: Базовые данные для оформления заказа (адрес и оплата).

Поля:

payment: 'card' | 'cash' - Выбранный способ оплаты.

address: string - Адрес доставки.

Тип IBuyer

Назначение: Контактные данные покупателя.

Поля:

email: string - Электронная почта.

phone: string - Телефон.

Тип IOrderPayload

Назначение: Полные данные, отправляемые на сервер при оформлении заказа.

Поля:

payment: 'card' | 'cash' - Способ оплаты.

address: string - Адрес доставки.

email: string - Контактный email.

phone: string - Контактный телефон.

total: number - Общая стоимость заказа.

items: string[] - Массив ID товаров в заказе.

Тип IOrderResult

Назначение: Ответ сервера после успешного оформления заказа.

Поля:

id: string - Идентификатор заказа.

total: number - Общая стоимость.

Тип ApiListResponse<T>

Назначение: Обобщенный ответ сервера при запросе списка данных.

Поля:

total: number - Общее количество элементов.

items: T[] - Массив элементов типа T.

Тип FormErrors

Назначение: Объект для хранения ошибок валидации форм.

Поля:

[key: string]: string - Сообщение об ошибке, где ключ — имя поля.

Классы слоя «Модели» (Model)

Класс ProductsModel

Назначение: Хранит состояние всего каталога товаров, управляет списком товаров и данными выбранного для просмотра товара.

Конструктор:

constructor(): Не принимает параметров, управление состоянием осуществляется через EventEmitter.

Поля:

private _items: IProduct[]: Список всех товаров в каталоге.

private _selectedProduct: IProduct | null: Данные выбранного для просмотра товара.

Методы:

getItems(): IProduct[]: Возвращает массив всех товаров.

setItems(data: IProduct[]): void: Устанавливает список товаров, полученных с сервера.

getItemById(id: string): IProduct | undefined: Выполняет поиск товара по его уникальному ID.

setSelectedProduct(id: string): void: Устанавливает выбранный товар по ID.

getSelectedProduct(): IProduct | null: Возвращает выбранный товар.

Класс CartModel

Назначение: Управляет списком товаров, добавленных в корзину, и рассчитывает общую стоимость заказа.

Конструктор:

constructor(): Не принимает параметров.

Поля:

private _items: ICartItem[]: Список товаров, находящихся в корзине.

Методы:

getItems(): ICartItem[]: Возвращает текущий список товаров в корзине.

add(product: IProduct): void: Добавляет товар (IProduct) в корзину (при условии, что цена не null).

remove(id: string): void: Удаляет товар из корзины по его ID (string).

clear(): void: Полностью очищает корзину.

getTotal(): number: Рассчитывает и возвращает общую стоимость товаров в корзине.

getCount(): number: Возвращает общее количество товаров в корзине.

Класс BuyerModel

Назначение: Хранит данные покупателя и заказа (адрес, способ оплаты, контакты) и отвечает за их валидацию.

Конструктор:

constructor(): Не принимает параметров.

Поля:

private _order: Partial<IOrder>: Частичные данные по оплате и адресу.

private _buyer: Partial<IBuyer>: Частичные контактные данные покупателя.

Методы:

setPayment(value: 'card' | 'cash'): void: Устанавливает способ оплаты.

setAddress(value: string): void: Устанавливает адрес доставки.

setEmail(value: string): void: Устанавливает Email покупателя.

setPhone(value: string): void: Устанавливает телефон покупателя.

validateAddressPayment(): FormErrors: Проверяет наличие способа оплаты и адреса. Возвращает объект с ошибками (FormErrors).

validateContacts(): FormErrors: Проверяет наличие email и телефона. Возвращает объект с ошибками (FormErrors).

getOrderPayload(items: string[], total: number): IOrderPayload: Собирает все данные (items - string[] ID товаров, total - number) в объект для отправки на сервер.

reset(): void: Очищает все данные заказа и контактов.

Класс коммуникационного слоя

Класс WebLarekApi

Назначение: Унаследован от базового Api и реализует специфические для приложения методы взаимодействия с бэкендом "ВебЛарек".

Конструктор:

constructor(baseURL: string, options?: RequestInit): Вызывает конструктор базового класса, передавая базовый URL сервера.

Поля: Отсутствуют (использует поля базового Api).

Методы:

getProducts(): Promise<IProduct[]>: Выполняет GET-запрос (/product/) и возвращает промис с массивом товаров (IProduct[]).

postOrder(order: IOrderPayload): Promise<IOrderResult>: Выполняет POST-запрос (/order/), отправляя данные заказа (IOrderPayload), и возвращает промис с результатом оформления заказа (IOrderResult).

Я понимаю, что предыдущее сообщение оборвалось. Вы хотели получить полную документацию для Классов слоя «Представления» (View), которая следует за описанием методов WebLarekApi.

Вот полный текст документации, которую вам нужно вставить в ваш README.md после предоставленного фрагмента.

📚 Документация классов слоя «Представления» (View)
«КОММЕНТАРИЙ РЕВЬЮЕРА: Документацию нужно дополнить описанием классов представления в таком же формате, как описали классы моделей»

Классы слоя «Представления» (View)
Классы представления отвечают за отображение данных (полученных от Model через Presenter) в пользовательском интерфейсе и инициирование событий при взаимодействии пользователя. Они наследуются от базового класса Component.

Класс Header (Отображение заголовка и корзины)
Назначение: Отображает счетчик товаров в корзине и обрабатывает клики по иконке корзины для её открытия.

Конструктор: constructor(events: IEvents, container: HTMLElement): Принимает брокер событий (IEvents) и корневой DOM-элемент компонента.

Поля: protected counterElement: HTMLElement: Элемент, отображающий количество товаров в корзине. protected basketButton: HTMLButtonElement: Кнопка/иконка корзины.

Методы: set counter(value: number): Устанавливает и отображает новое значение счетчика товаров в корзине.

События, которые генерирует:

EVENTS.BASKET_OPEN: Генерируется при клике на иконку корзины.

Класс Gallery (Отображение каталога)
Назначение: Отображает список карточек товаров (CardGallery) на главной странице.

Конструктор: constructor(container: HTMLElement): Принимает корневой DOM-элемент (<main class="gallery">).

Поля: protected catalogElement: HTMLElement: Контейнер для размещения карточек товаров.

Методы: set catalog(items: HTMLElement[]): Принимает массив DOM-элементов карточек товаров и вставляет их в контейнер каталога, полностью заменяя предыдущее содержимое.

Класс Card<T> (Базовая карточка товара)
Назначение: Базовый класс для всех типов карточек товаров. Реализует общую логику отображения заголовка, цены и обработки клика. Наследуется от Component<T & TCardBase>.

Конструктор: constructor(container: HTMLElement, actions?: ICardActions): Принимает DOM-элемент шаблона и опциональный объект с обработчиками действий (onClick, onBuy, onRemove).

Поля: protected titleElement: HTMLElement: Элемент, отображающий название товара. protected priceElement: HTMLElement: Элемент, отображающий цену товара. protected buttonElement?: HTMLButtonElement: Опциональная кнопка действия (зависит от наследника).

Методы: set title(value: string): Устанавливает текст заголовка. set price(value: number | null): Устанавливает и форматирует цену (отображает "Бесценно", если null).

События, которые генерирует:

Через actions.onClick: При клике на саму карточку (обычно приводит к EVENTS.CARD_SELECT).

Через actions.onBuy: При клике на кнопку внутри карточки (обычно приводит к EVENTS.CARD_BUY).

Класс CardGallery (Карточка в каталоге)
Назначение: Отображает товар в общем списке каталога. Наследует функциональность от Card.

Конструктор: constructor(container: HTMLElement, actions?: ICardActions): Вызывает конструктор родителя.

Поля: protected categoryElement: HTMLElement: Элемент для отображения категории. protected imageElement: HTMLImageElement: Элемент для отображения изображения.

Методы: set category(value: string): Устанавливает название категории, автоматически применяя модификатор SCSS. set image(value: string): Устанавливает URL изображения.

Класс CardPreview (Детальный просмотр товара)
Назначение: Отображает полную информацию о товаре в модальном окне. Наследует функциональность от Card.

Конструктор: constructor(container: HTMLElement, actions?: ICardActions): Вызывает конструктор родителя.

Поля: protected descriptionElement: HTMLElement: Элемент для полного описания товара.

Методы: set description(value: string): Устанавливает полное описание товара. set inCart(value: boolean): Управляет состоянием кнопки (добавить/удалить) в зависимости от того, находится ли товар в корзине.

Класс CardInCart (Строка товара в корзине)
Назначение: Отображает отдельную строку товара внутри корзины. Наследует функциональность от Card.

Конструктор: constructor(container: HTMLElement, actions?: ICardActions): Принимает элемент шаблона и обработчик onRemove.

Поля: protected indexElement: HTMLElement: Элемент, отображающий порядковый номер товара.

Методы: set index(value: number): Устанавливает порядковый номер товара.

Класс BasketView (Корзина)
Назначение: Отображает список товаров в корзине (CardInCart), общую стоимость и кнопку оформления заказа.

Конструктор: constructor(container: HTMLElement, onOrder: () => void): Принимает элемент шаблона и функцию, которая будет вызвана при клике на кнопку оформления.

Поля: protected listElement: HTMLElement: Контейнер для строк товаров. protected totalElement: HTMLElement: Элемент для отображения общей стоимости. protected orderButton: HTMLButtonElement: Кнопка "Оформить заказ".

Методы: set items(items: HTMLElement[]): Принимает массив DOM-элементов строк корзины и вставляет их в список. set total(value: number): Устанавливает и отображает общую стоимость. set empty(value: boolean): Деактивирует кнопку оформления, если корзина пуста.

Класс Modal
Назначение: Управляет состоянием модального окна (открытие/закрытие) и отображением контента внутри него.

Конструктор: constructor(container: HTMLElement): Принимает корневой DOM-элемент модального окна.

Поля: protected closeButton: HTMLButtonElement: Кнопка закрытия модального окна. protected contentElement: HTMLElement: Контейнер для встраивания содержимого.

Методы: open(content: HTMLElement): Открывает модальное окно и вставляет в него переданный контент. close(): Закрывает модальное окно.

События, которые генерирует:

Закрытие также происходит при клике на оверлей или клавише Escape (обычно через обработчик в main.ts).

Класс OrderAddressPayment
Назначение: Отображает и обрабатывает форму выбора способа оплаты и адреса доставки.

Конструктор: constructor(container: HTMLElement, events: IEvents): Принимает элемент шаблона и брокер событий.

Методы: set address(value: string): Устанавливает значение поля адреса. set payment(value: TPayment): Управляет активным состоянием кнопок оплаты (card/cash). set valid(value: boolean): Управляет активностью кнопки "Далее". set errors(value: Partial<Record<'payment' | 'address', string>>): Отображает ошибки валидации.

События, которые генерирует:

ORDER_INPUT_CHANGE: Генерируется при изменении поля адреса или выборе способа оплаты.

ORDER_ADDRESS_PAYMENT_NEXT: Генерируется при клике на кнопку "Далее".

Класс OrderEmailPhone
Назначение: Отображает и обрабатывает форму ввода контактных данных (Email и телефон).

Конструктор: constructor(container: HTMLElement, events: IEvents): Принимает элемент шаблона и брокер событий.

Методы: set email(value: string): Устанавливает значение поля Email. set phone(value: string): Устанавливает значение поля телефона. set valid(value: boolean): Управляет активностью кнопки "Оплатить". set errors(value: Partial<Record<'email' | 'phone', string>>): Отображает ошибки валидации.

События, которые генерирует:

ORDER_INPUT_CHANGE: Генерируется при изменении полей Email или телефона.

ORDER_EMAIL_PHONE_PAY: Генерируется при клике на кнопку "Оплатить".

Класс OrderSuccess
Назначение: Отображает сообщение об успешном оформлении заказа.

Конструктор: constructor(container: HTMLElement, onClose: () => void): Принимает элемент шаблона и функцию, которая вызывается при закрытии окна.

Методы: set total(value: number): Устанавливает и отображает итоговую списанную сумму.

События, которые генерирует:

Вызывает переданную функцию onClose при клике на кнопку закрытия.